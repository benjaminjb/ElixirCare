defmodule Elixirtask.ViewClient do
  # require Logger
  # use HTTPoison.Base

  # alias Elixirtask.Config

  # def process_url(query_string) do
  #   endpoint = Config.get(:view)[:endpoint]
  #   url = endpoint <> "?" <> query_string

  #   Logger.info "Querying view: #{url}"

  #   url
  # end

  # def process_response_body(body) do
  #   body |> Poison.Parser.parse!
  # end
end